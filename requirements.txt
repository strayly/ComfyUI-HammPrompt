# 依赖说明
# ---------------------------------------------------------------
# 本节点运行只需 ComfyUI 自带环境，无需额外依赖即可使用在线 API 扩写
# 与本地 llama.cpp / transformers（均走标准库 urllib 与懒加载导入）。
#
# 仅在你想用「本地模型（GGUF / HF）」扩写时才需要下面这个可选依赖：
#   llama-cpp-python
# 它体积较大且与平台/CUDA 强相关，因此不写进强制依赖，请按需手动安装：
#   pip install llama-cpp-python
